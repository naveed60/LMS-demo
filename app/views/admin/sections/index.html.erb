<%= turbo_frame_tag 'main' do %>
<%=stylesheet_link_tag "section",  "data-turbo-track": "reload"%>
<body>

<center>
<h1>Sections</h1>
<div>
  <p>
    <%= link_to "Add Section", new_admin_section_path, class: "btn btn-outline-info",  
                  style:"background-color:#6f42c1; border-color:#6f42c1;
                   color:white; padding:0.6rem 2rem; border-radius:50rem;
                   font-weight:600; border:none;"%>
  </p>
  </center>
</div>
<table align="center" id="enrollment" border="3" layout= "fixed" width=" 100%"  >
<tr>
    <th>Course</th>
    <th>Section</th>
    <th>Teacher</th>
    <th>Semester</th>
    <th>Semester No</th>
    <th>Edit</th>
    <th>Delete</th>
  </tr>

  <% @sections.each do |s| %>
    <tr>
        <td><%= s.course.name %></td>
        <td><%= s.name%></td>
        <td><%= s.teacher.name %></td>
        <td><%= s.semester.semester_type %></td>
        <td><%= s.semester.semester_number%></td>
        <td><%= link_to 'Edit', edit_admin_section_path(s), class: "btn btn-outline-info" %></td>
        <td><%= link_to "Delete", admin_section_path(s), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  }, class: "btn btn-outline-info" %></td>
      </a>
    </tr>
  <% end %>
  <%end%>